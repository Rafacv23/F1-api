export const generateId = (year) => `f1_${year}`
export const generateName = (year) => `${year} Formula 1 World Championship`
export const generateChampionshipId = (year) => `f1_${year}`
export const generateRaceId = (circuitId, year) => `${circuitId}_${year}`
