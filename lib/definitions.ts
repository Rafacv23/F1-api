export type EndpointType = {
  url: string
  title: string
  description: string
  params: string
  example: number
}

export type FaqType = {
  question: string
  answer: string
}

export type TermType = {
  title: string
  description: string
}
